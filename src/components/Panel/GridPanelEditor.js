import { useState } from 'react';
import GridPanel from './GridPanel';

export default function GridPanelEditor(props) {
	// const {
	// 		Editor,
	// 		gridProps = {},
	// 	} = props,
	// 	[showEditor, setShowEditor] = useState(false),
	// 	[selectedRow, setSelectedRow] = useState(null),
	// 	onSelect = (row) => {
	// 		setSelectedRow(row);
	// 		setShowEditor(true);
	// 	},
	// 	onCloseEditor = () => {
	// 		setShowEditor(false);
	// 	};

	// return showEditor ? <Editor onClose={onCloseEditor} entity={selectedRow} /> : <GridPanel {...gridProps} onSelect={onSelect} hideRightColumn={false} />;
}
