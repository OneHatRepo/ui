import withSelection from '../Hoc/withSelection';
import Grid from './Grid';


// function SelectionGrid(props) {
// 	const {
// 		} = props,
// 		onSelect= (item, index) => {

// 		};


// 	return <Grid onSelect={onSelect} {...props}>
			
// 			</Grid>;
// }



export default withSelection(Grid);