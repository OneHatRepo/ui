import { StyleSheet } from 'react-native';

export const styles = {
	nsResize: {
		cursor: 'ns-resize',
	},
	ewResize: {
		cursor: 'ew-resize',
	},
	shiftUpAbove: {
		transform: 'translateY(-100%)',
	},
};
export default StyleSheet.create(styles);
